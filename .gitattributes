# The map is generated by scripts/render_map.py, which writes LF. Without
# this, a Windows checkout converts it to CRLF and every line of the map shows
# as changed on the next render — and `--check` compares text that git and the
# working tree disagree about.
*.svg text eol=lf
